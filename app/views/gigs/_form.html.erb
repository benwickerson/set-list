<%= form_for(@gig) do |f| %>
  <% if @gig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gig.errors.count, "error") %> prohibited this gig from being saved:</h2>

      <ul>
      <% @gig.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gig %><br>
    <%= f.text_field :gig, class:"form-control"  %>
  </div>
  <div class="field ">
    <%= f.label :gig_date %><br>
    <%= f.date_select :gig_date, class:"form-control"  %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
